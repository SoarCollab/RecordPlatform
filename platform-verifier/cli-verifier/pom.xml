<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.flying</groupId>
        <artifactId>platform-verifier</artifactId>
        <version>0.0.2-SNAPSHOT</version>
    </parent>

    <artifactId>platform-verifier-cli</artifactId>
    <name>platform-verifier-cli</name>
    <description>Executable public proof verifier CLI</description>

    <dependencies>
        <dependency>
            <groupId>cn.flying</groupId>
            <artifactId>platform-verifier-sdk</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.flying</groupId>
            <artifactId>platform-verifier-sdk</artifactId>
            <version>${project.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>record-platform-verifier</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>cn.flying.verifier.cli.VerifierCli</mainClass>
                    <classifier>exec</classifier>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
