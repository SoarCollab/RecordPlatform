/**
 * API v1 版本的 DTO 定义包
 *
 * <p>此包用于存放 API v1 版本的数据传输对象，实现 API 版本化管理。
 * 当需要发布 v2 版本时，可以在 v2 包中定义新的 DTO，同时保持 v1 兼容。
 *
 * <p>目前 v1 版本的 DTO 直接使用 cn.flying.dao.vo 包中的类，
 * 未来版本迭代时可以在此包中创建专属的 v1 DTO 定义。
 *
 * @since 1.0
 */
package cn.flying.api.dto.v1;
