<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.11</version>
        <relativePath/>
        <!-- lookup parent from repository -->
    </parent>
    <groupId>cn.flying</groupId>
    <artifactId>platform-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>RecordPlatform-backend</name>
    <description>存证平台后端服务</description>
    <properties>
        <java.version>21</java.version>
        <hutool.version>5.8.32</hutool.version>
        <mybatis-plus-bom.version>3.5.9</mybatis-plus-bom.version>
        <testcontainers.version>1.20.4</testcontainers.version>
        <jacoco.version>0.8.12</jacoco.version>
        <maven-failsafe-plugin.version>3.2.5</maven-failsafe-plugin.version>
        <!--
            Keep commons-compress consistent on test/runtime classpath.
            Testcontainers may pull an older commons-compress which can break Apache POI at runtime (NoSuchMethodError).
         -->
        <commons-compress.version>1.28.0</commons-compress.version>
        <!--
            Keep commons-io consistent on test/runtime classpath.
            Apache POI may pull an older commons-io which can break Testcontainers file copy (NoSuchMethodError: FileTimes.toUnixTime).
         -->
        <commons-io.version>2.20.0</commons-io.version>
        <!--
            commons-compress 1.28+ requires commons-lang3 containing ArrayFill (>= 3.14.0).
            Testcontainers (docker-java) may require SystemProperties.getUserName(String) (>= 3.17.0).
         -->
        <commons-lang3.version>3.18.0</commons-lang3.version>
        <!--
            Mockito inline mock maker relies on Byte Buddy instrumentation.
            On some macOS/JDK builds, runtime self-attach is not available; pre-attaching the agent fixes it.
         -->
        <byte-buddy-agent.version>1.14.19</byte-buddy-agent.version>
    </properties>
    <!--  设置打包方式pom  -->
    <packaging>pom</packaging>

    <modules>
        <module>backend-api</module>
        <module>backend-common</module>
        <module>backend-dao</module>
        <module>backend-service</module>
        <module>backend-web</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!--    项目子模块    -->
            <dependency>
                <groupId>cn.flying</groupId>
                <artifactId>backend-dao</artifactId>
                <version>0.0.1-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>cn.flying</groupId>
                <artifactId>backend-api</artifactId>
                <version>0.0.1-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>cn.flying</groupId>
                <artifactId>backend-common</artifactId>
                <version>0.0.1-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>cn.flying</groupId>
                <artifactId>backend-service</artifactId>
                <version>0.0.1-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>cn.flying</groupId>
                <artifactId>backend-web</artifactId>
                <version>0.0.1-SNAPSHOT</version>
            </dependency>
            <!--     共享库     -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-bom</artifactId>
                <version>${mybatis-plus-bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- TestContainers BOM -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Force commons-compress version to avoid POI runtime incompatibility in tests -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-compress</artifactId>
                <version>${commons-compress.version}</version>
            </dependency>
            <!-- Force commons-io version to avoid Testcontainers/POI runtime incompatibility in tests -->
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons-io.version}</version>
            </dependency>
            <!-- Ensure commons-lang3 matches commons-compress runtime needs (ArrayFill) -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>enforce-java-21</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[21,22)</version>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!--
                Ensure Mockito inline mock maker can load without relying on runtime self-attach.
                JDK 21+ may restrict dynamic agent loading; tests using MockedStatic require it.
             -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>${argLine} -javaagent:${settings.localRepository}/net/bytebuddy/byte-buddy-agent/${byte-buddy-agent.version}/byte-buddy-agent-${byte-buddy-agent.version}.jar -Djdk.attach.allowAttachSelf=true -XX:+EnableDynamicAgentLoading</argLine>
                </configuration>
            </plugin>
            <!-- JaCoCo for test coverage -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <configuration>
                    <excludes>
                        <!-- Avoid JaCoCo instrumenting huge 3rd-party parser methods (can trigger MethodTooLargeException) -->
                        <exclude>net/sf/jsqlparser/**</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <!-- Report after integration tests (includes IT coverage) -->
                    <execution>
                        <id>report-it</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            Integration tests profile.
            Run with: mvn -f platform-backend/pom.xml verify -Pit
            Integration tests should be named *IT.java and can use Testcontainers.
        -->
        <profile>
            <id>it</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <version>${maven-failsafe-plugin.version}</version>
                        <configuration>
                            <includes>
                                <include>**/*IT.java</include>
                            </includes>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
