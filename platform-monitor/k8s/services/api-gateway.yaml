apiVersion: apps/v1
kind: Deployment
metadata:
  name: monitor-api-gateway
  namespace: monitor-system
  labels:
    app: monitor-api-gateway
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: monitor-api-gateway
  template:
    metadata:
      labels:
        app: monitor-api-gateway
        version: v1
      annotations:
        sidecar.istio.io/inject: "true"
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: monitor-api-gateway
      containers:
      - name: api-gateway
        image: monitor/api-gateway:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: SPRING_PROFILES_ACTIVE
          valueFrom:
            configMapKeyRef:
              name: monitor-config
              key: SPRING_PROFILES_ACTIVE
        - name: JAVA_OPTS
          valueFrom:
            configMapKeyRef:
              name: monitor-config
              key: JAVA_OPTS
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: monitor-config
              key: REDIS_HOST
        - name: REDIS_PORT
          valueFrom:
            configMapKeyRef:
              name: monitor-config
              key: REDIS_PORT
        - name: NACOS_SERVER_ADDR
          valueFrom:
            configMapKeyRef:
              name: monitor-config
              key: NACOS_SERVER_ADDR
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: monitor-secrets
              key: JWT_SECRET
        - name: SPRING_SLEUTH_ZIPKIN_BASE_URL
          valueFrom:
            configMapKeyRef:
              name: monitor-config
              key: SPRING_SLEUTH_ZIPKIN_BASE_URL
        - name: SPRING_SLEUTH_SAMPLER_PROBABILITY
          valueFrom:
            configMapKeyRef:
              name: monitor-config
              key: SPRING_SLEUTH_SAMPLER_PROBABILITY
        - name: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
          valueFrom:
            configMapKeyRef:
              name: monitor-config
              key: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
        - name: MANAGEMENT_METRICS_EXPORT_PROMETHEUS_ENABLED
          valueFrom:
            configMapKeyRef:
              name: monitor-config
              key: MANAGEMENT_METRICS_EXPORT_PROMETHEUS_ENABLED
        - name: GATEWAY_RATE_LIMIT_ENABLED
          valueFrom:
            configMapKeyRef:
              name: monitor-config
              key: GATEWAY_RATE_LIMIT_ENABLED
        - name: GATEWAY_CIRCUIT_BREAKER_ENABLED
          valueFrom:
            configMapKeyRef:
              name: monitor-config
              key: GATEWAY_CIRCUIT_BREAKER_ENABLED
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 120
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - name: tmp-volume
          mountPath: /tmp
        - name: logs-volume
          mountPath: /app/logs
      volumes:
      - name: tmp-volume
        emptyDir: {}
      - name: logs-volume
        emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: monitor-api-gateway-service
  namespace: monitor-system
  labels:
    app: monitor-api-gateway
spec:
  selector:
    app: monitor-api-gateway
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: monitor-api-gateway-hpa
  namespace: monitor-system
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: monitor-api-gateway
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 10
        periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60